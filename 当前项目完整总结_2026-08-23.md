# A01 AI 能力测评智能体当前项目完整总结

> 更新时间：2026-08-23
> 当前版本：v1.5.0
> 功能代码基线：`4420ba1 feat: refine review and growth management UX`（本文档与清理提交位于其后）
> GitHub：https://github.com/Artaue-284/a01-ai-ability-assessment
> 本地目录：`E:\学校文件\数字马力\a01-ai-ability-assessment-main\a01-ai-ability-assessment-main`

## 一、总体结论

项目目前已经从早期原型发展为一个可以完整演示的三角色 AI 能力测评平台，具备学员测评、教师教研管理、企业岗位发布与投递、成长追踪、人工复核、题库管理、模型接入和公网演示等主要流程。

当前最适合的定位是：

> **功能闭环基本完成、演示数据较完整、能够用于比赛答辩和产品展示，但真实教育测量效度、真实教师评分一致性、真实多模态理解和生产级长期部署仍待验证。**

需要始终区分以下两类成果：

- 已验证的工程成果：代码可运行、接口可用、权限可控、数据库闭环、浏览器流程和自动测试通过。
- 尚未取得的研究证据：真实学生试测、真实教师盲评、真实题目曝光质量、等级阈值校准和长期生产运行数据。

## 二、当前版本与访问方式

### 2.1 代码版本

- 主仓库：`Artaue-284/a01-ai-ability-assessment`
- 当前分支：`main`
- 当前最新提交：`4420ba1`
- 当前工作区在生成本文档前无未提交代码修改。

最近主要提交如下：

| 提交 | 主要内容 |
|---|---|
| `4420ba1` | 精简用户界面文案；修正人工复核口径；增加复核和题库页码跳转；成长曲线支持点击切换 |
| `9fab3d8` | 人工复核每页 5 道；增加 50 次成长历史演示 |
| `81e81ec` | 创建 5 个演示岗位并生成 500 次学员投递 |
| `8ec7d53` | 创建 3 个教师账号和 2 个企业账号 |
| `a08aa3a` | 增加多模态与 AI 实操演示流程 |
| `af42b13` | 增加安全公网演示部署配置 |

### 2.2 公网演示方式

项目不在文档中固化 Quick Tunnel 地址。运行 `启动远程测试.bat` 后会生成当次有效的 `https://*.trycloudflare.com` 地址；该地址应在演示前现场确认。

2026-08-23 的临时公网回归已经验证：

- 首页返回 HTTP 200；
- `/api/status` 返回 `status=ok`；
- HTTPS、HSTS、CSP、`X-Frame-Options: DENY` 等安全响应头生效；
- 非白名单 Host 请求返回 HTTP 400。

注意：这是 Cloudflare Quick Tunnel 临时演示地址，不是长期云服务器。关闭本机 Python 服务或 cloudflared 后地址会失效，下次启动通常会变化。仓库中的 `render.yaml` 和 `Dockerfile` 已为正式云平台部署做好基础准备，但目前没有证据表明已经建立持续在线的 Render 正式实例。

## 三、技术架构

### 3.1 后端与存储

- Web 框架：FastAPI
- 运行方式：Uvicorn
- 数据库：SQLite
- API 文档：`/docs`
- 健康检查：`/api/status`
- 测评、答题、账号、人工复核、岗位与投递数据均已持久化。

### 3.2 前端

- 单页原生 HTML、CSS、JavaScript 前端；
- 学员、教师、企业共用一套页面入口；
- 支持桌面浏览器与手机尺寸；
- 测评中断后可在同一浏览器继续；
- 题号导航支持阶段内自由切换，并在进入下一阶段后继续自适应测评。

### 3.3 AI 接入状态

当前运行时 `/api/status` 显示：

| 能力 | 当前状态 | 说明 |
|---|---|---|
| 开放题评分 | `ant-line-api` | 当前队友配置的 ant-line 通道已加载 |
| 连续对话助手 | `ant-line-api` | 当前可调用真实模型；失败时有透明降级逻辑 |
| 百宝箱 | `not-configured` | 当前进程没有加载百宝箱令牌 |
| 文档分析 | `configured` | 支持文档、PDF、CSV 等证据提取与分析流程 |
| 视觉分析 | `not-configured` | 尚未配置真正理解图片内容的视觉模型 |
| AI 实操 Agent | `configured` | 已有安全工具调用与白名单执行框架 |

这些状态依赖本机或部署环境中的配置，换机器、删除本地配置或重新部署后应再次通过 `/api/status` 核对。本文档不记录任何模型密钥。

## 四、题库与测评机制

### 4.1 当前题库规模

当前题库共 **301 道题**，结构校验通过，无结构错误或警告。

按维度分布：

| 维度 | 题量 |
|---|---:|
| AI 基础认知 | 51 |
| 工具应用 | 50 |
| 结果评估 | 50 |
| 提示词设计 | 50 |
| 人机协同 | 50 |
| 伦理与安全 | 50 |

按题型分布：

| 题型 | 题量 |
|---|---:|
| 单项选择 | 181 |
| 判断题 | 18 |
| 开放作答 | 24 |
| 实操任务 | 24 |
| 对话任务 | 18 |
| 代码任务 | 18 |
| 图像鉴别 | 18 |

按难度分布：难度 1 共 80 道，难度 2 共 137 道，难度 3 共 84 道。

### 4.2 抽题与自适应逻辑

- 支持 15、18、25 题三种测评深度；
- 按“六维 × 题型 × 难度”进行分层随机抽题；
- 同一次测评内依据题目 ID 去重；
- 前段覆盖六个维度，中段向低置信度和薄弱维度补测；
- 后段可以纳入开放题、实操题、对话题、代码题和图像题；
- 达到目标题量或提前停止条件后生成报告。

50 次成长演示并不是重复提交同一张卷。当前演示数据保证每名学员的 50 次完整题目组合不同，但不同测评之间允许复用部分单题；这符合题库规模和成长演示的实际需求。

### 4.3 题库管理

教师端现已支持：

- 新建和编辑题目；
- 启用、停用题目；
- 版本记录；
- AI 生成草稿后由教师编辑确认；
- 按题目 ID、题干或标签搜索；
- 查看曝光次数、均分、区分度与质量标记；
- 每页显示 10 道题；
- 上一页、下一页和输入页码直接跳转。

## 五、三角色业务闭环

### 5.1 学员端

学员无需平台账号即可：

1. 填写姓名或匿名代号、班级或团队；
2. 选择 15、18 或 25 题模式；
3. 完成客观题、开放题、对话题和实操类题目；
4. 查看综合分、六维得分、等级、证据和提升建议；
5. 查看历史测评和成长档案；
6. 在报告页浏览岗位广场；
7. 明确授权共享能力画像摘要后投递岗位。

### 5.2 教师端

教师账号登录后可以：

- 查看数据概览、班级表现与最近完成记录；
- 管理题库及其版本；
- 对主观题进行独立人工评分；
- 查看双评、一致性指标和争议裁决；
- 选择学员查看成长曲线；
- 管理教师和企业账号；
- 查看班级岗位投递统计；
- 导出作答、反馈、题目统计等 CSV 数据。

人工复核现为每页 5 道，并支持输入页码跳转。成长追踪初始默认只显示“综合”曲线，教师可以点击综合分或任一能力维度，切换为对应的单条曲线。

### 5.3 企业端

企业账号登录后可以：

- 查看达到隐私阈值后的匿名群体画像；
- 查看和启停岗位能力模板；
- 发布岗位并关联能力模板；
- 查看学员主动授权的投递；
- 查看岗位匹配分与解释；
- 关闭岗位。

企业端不能访问学员原始答案和完整个人报告，岗位匹配结果也不应作为自动录用或自动淘汰的唯一依据。

## 六、当前演示数据

数据库当前统计如下：

| 数据项 | 当前数量 | 说明 |
|---|---:|---|
| 用户记录 | 101 | 其中 100 名为仿真演示学员，另 1 条为本地使用记录，不能据此认定为真实受试者 |
| 测评记录 | 5,002 | 5,000 次仿真演示测评，另有 2 次本地记录 |
| 已完成测评 | 5,001 | 5,000 次仿真演示 + 1 次本地完成记录 |
| 进行中测评 | 1 | 本地未完成记录 |
| 答题记录 | 125,029 | 其中 125,000 条来自仿真演示数据 |
| 教师账号 | 3 | 演示账号，不代表 3 名教师已参加真实评分 |
| 企业账号 | 2 | 演示账号，不代表 2 家企业已完成真实业务验证 |
| 岗位模板 | 3 | 用于企业端演示 |
| 已发布岗位 | 5 | 当前均为开放岗位 |
| 岗位投递 | 500 | 100 名演示学员均投递 5 个岗位 |
| 人工评分记录 | 48 | 24 道答案各有 2 条演示评分 |
| 双评答案 | 24 | 满足“不少于 20 道”的答辩展示需求 |
| 争议裁决 | 4 | 用于展示双评争议处理流程 |

每名演示学员有 50 次已完成测评，可用于展示成长曲线。演示数据在数据库内部标记为 `synthetic_demo`，题目质量统计会排除这些数据，避免仿真作答污染真实曝光率、得分率和区分度。

网页没有突出显示“仿真演示”标签，以满足当前演示要求；但在研究报告、比赛材料和口头答辩中仍应如实说明数据来源，不应把它表述为真实学生试测。

## 七、人工复核口径修正

此前人工复核页面把 5,000 次仿真测评中的全部主观题都计入队列，导致约 25,000 道题出现在人工复核中，不符合演示需求。

当前口径已经修正为：

- 非仿真来源的主观题全部进入复核队列；
- 仿真数据只保留已抽中并具有人工评分记录的演示样本；
- 当前复核队列共 29 道，而不是约 25,000 道；
- 其中保留 24 道双评样本，能够演示独立评分、一致性指标和争议裁决。

这里的 48 条人工评分和 24 道双评答案仍属于演示数据，不是两名真实教师独立盲评产生的研究证据。

## 八、多模态与真实 AI 实操现状

### 8.1 已完成的工程能力

- 支持上传图片、PDF、文本和数据文件作为作答证据；
- 支持文档、文本、CSV 等内容提取；
- 已实现多模态分析接口和对应自动测试；
- 已实现实操 Agent 的工具白名单；
- 已实现安全算术工具、未知工具拒绝和工具调用循环；
- 模型不可用时会透明降级，不把本地规则结果冒充为真实模型结果。

### 8.2 尚未完成的部分

- 当前 `vision_analysis=not-configured`，模型尚不能真正理解上传图片的视觉语义；
- 当前数据库中 `evidence_files`、`evidence_analyses` 和 `practical_runs` 仍为 0，尚无保留下来的完整实操演示运行证据；
- 还没有受控浏览器操作环境；
- 还没有可执行通用代码的隔离沙箱；
- 还没有 Word、Excel、PowerPoint 等办公工具的受控操作环境；
- 当前工具调用框架不能等同于完整的真实 AI 实操考场。

因此，“多模态上传与分析框架”和“安全实操 Agent 框架”可以认定为已实现；“真正视觉理解”和“完整真实 AI 操作环境”只能认定为部分完成。

## 九、成长追踪

当前成长追踪已具备：

- 学员列表和已完成测评次数；
- 每名演示学员 50 个历史测评点；
- 综合分成长曲线；
- 六个能力维度的独立成长曲线；
- 点击按钮切换当前显示指标；
- 默认只显示综合曲线，避免七条曲线同时出现造成视觉混乱；
- 50 个时间点的横轴标签抽样显示，减少重叠。

需要注意：当前曲线反映的是仿真演示增长轨迹，不能用来证明真实教学干预有效。

## 十、测试与回归证据

2026-08-23 重新执行验证结果：

- Python 自动测试：63 项全部通过；
- Python AST 解析：49 个 Python 文件通过；
- 题库结构：301 道题，校验通过；
- FastAPI 应用启动：通过；
- 本地首页与 `/api/status`：通过；
- 教师端桌面浏览器回归：通过；
- 390 × 844 手机视口回归：通过；
- 手机页面无页面级横向溢出；
- 登录角色切换后账号、密码清空：通过；
- 人工复核每页 5 道及页码跳转：通过；
- 题库每页 10 道及页码跳转：通过；
- 成长曲线默认综合、点击切换到能力维度：通过；
- 演示学员 50 个成长历史点：通过；
- 公网 HTTPS 与安全响应头：通过；
- 错误 Host 拦截：通过。

这些测试证明当前版本具备可运行性和演示稳定性，但不能替代真实测量效度、教师评分效度和企业使用效果验证。

## 十一、安全与部署

### 11.1 当前已完成的安全措施

- 教师和企业使用独立角色账号；
- 账号口令以 PBKDF2 哈希保存；
- 会话令牌有有效期；
- 教师、企业接口按角色鉴权；
- 管理员密钥与企业密钥可通过环境变量注入；
- 公网模式要求高强度环境密钥；
- 登录接口具备基于客户端地址的限流；
- 支持精确 Host 白名单；
- 设置 CSP、HSTS、禁止页面嵌入、MIME 嗅探保护等响应头；
- API 响应设置 `Cache-Control: no-store`；
- 数据库、访问密钥文件和本地模型配置均已被 `.gitignore` 排除；
- 仓库中不应提交真实模型令牌和平台密钥。

### 11.2 尚未达到生产级的部分

- 当前公网服务仍依赖本机和临时 Quick Tunnel；
- SQLite 不适合高并发和多实例部署；
- 角色权限仍较粗，尚未形成细粒度 RBAC；
- 尚未接入专业密钥托管服务；
- 尚未形成自动数据库备份、恢复演练和异地容灾；
- 审计日志还不完整；
- 登录限流目前为单进程内存状态，多实例下不能共享；
- 尚未完成正式域名、稳定证书策略、监控告警和长期可用性验证。

## 十二、原剩余任务完成度复盘

| 原任务 | 当前判断 | 说明 |
|---|---|---|
| 真实学生小规模试测 | 未完成 | 当前 100 名学员均为仿真演示数据；本地普通记录不能证明是真实试测 |
| 真实教师双评 | 未完成 | 已有 24 道双评演示样本，但评分者为演示评分器，不是两名真实教师盲评 |
| 等级阈值校准 | 未完成 | 仍需结合真实学生和真实教师数据校准 L1～L5 边界，不应依据仿真数据直接修改 |
| 连续对话接入真实智能体 | 工程完成、真实性待持续验证 | 当前 ant-line 通道已加载并有真实模型接口；仍需保留调用证据并进行稳定性与质量验证 |
| 真正的多模态测评 | 部分完成 | 文档分析框架已完成，视觉模型当前未配置 |
| 真实 AI 实操环境 | 部分完成 | 安全工具 Agent 已完成，但受控浏览器、通用代码沙箱和办公工具环境未完成 |
| 扩展百宝箱生成题库 | 部分完成 | 题库已扩充到 301 道，AI 草稿—教师确认流程可演示；仍需批量生成并逐题真实教师审核 |
| 题库质量数据积累 | 未完成 | 仿真数据已被排除，但尚无足够真实曝光率、得分率、区分度和歧义反馈 |
| 教师端完整浏览器回归 | 已完成当前版本回归 | 桌面和 390 × 844 手机视口均通过；正式发布前仍应补测更多浏览器和真机 |
| 企业角色真实验证 | 演示闭环完成、真实验证未完成 | 5 个岗位和 500 次投递已可演示，但未取得真实企业人员确认 |
| 企业级安全与部署 | 演示级基本完成、生产级未完成 | 已有鉴权、限流、安全头、Host 白名单和部署配置；仍缺正式生产基础设施 |
| 最终竞赛演示 | 大部分完成 | 稳定演示数据和三角色路径已具备；仍需完善断网降级说明、演示脚本、视频与答辩材料 |

## 十三、建议的比赛演示路径

### 路径一：学员能力测评

1. 进入首页并选择测评题量；
2. 展示客观题、自适应选题和阶段导航；
3. 展示开放题或对话题中的 AI 助手；
4. 完成测评并查看综合分、六维报告和提升建议；
5. 进入岗位广场并授权投递。

### 路径二：教师教研管理

1. 使用教师账号登录；
2. 查看班级看板；
3. 在人工复核中展示每页 5 道和页码跳转；
4. 展示一道已有双评的答案和争议裁决；
5. 在题库管理中展示搜索、分页、AI 草稿、编辑和版本；
6. 在成长追踪中选择演示学员，展示综合曲线并点击切换六维曲线；
7. 展示班级岗位投递统计。

### 路径三：企业岗位闭环

1. 使用企业账号登录；
2. 查看匿名群体画像和岗位模板；
3. 展示 5 个已发布岗位；
4. 查看学员投递和匹配解释；
5. 强调企业只能看到授权后的必要信息，不能访问原始答卷。

### 断网或模型异常降级

- 公网地址不可用时改用 `http://127.0.0.1:8000` 本地演示；
- 模型调用失败时系统会降级到本地规则评分或模拟助手；
- 降级结果只能用于保证演示流程，不应描述为真实模型结果；
- 比赛现场应提前准备首页、学员报告、人工复核、成长曲线和企业岗位页面截图或录屏。

## 十四、下一阶段建议

按优先级建议继续推进：

1. 邀请 10～20 名不同能力层次的真实学生完成试测；
2. 邀请两名真实教师对同一批开放题独立盲评，计算 Kappa、相关性和裁决结果；
3. 使用真实数据重新评估并校准 L1～L5 阈值；
4. 配置并验证视觉模型，保留图片理解的真实调用证据；
5. 至少完成一次有持久化记录的证据上传与实操 Agent 运行；
6. 根据真实曝光率、得分率、区分度和歧义反馈修订题库；
7. 邀请真实企业人员审阅岗位模板、权重和匹配解释；
8. 部署到稳定云平台，配置固定域名、数据库备份和监控；
9. 完成三角色答辩脚本、断网预案、演示视频和正式提交材料。

## 十五、结论

当前项目已经具备较强的比赛演示完整性：题库、自适应测评、AI 对话、教师复核、成长曲线、企业岗位和安全公网访问均形成了可运行闭环。最新版本也解决了页面冗余文案、长列表分页、复核数量异常和成长曲线过度拥挤等展示问题。

下一阶段不应继续单纯堆叠仿真数据或机械增加题量，而应把主要精力放到真实学生、真实教师、真实企业、真实视觉模型和稳定云部署上。只有这些证据补齐后，项目才能从“可演示的工程产品”进一步发展为“具有可信测评效度和真实应用证据的正式产品”。
